<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-zinc-800 text-white box-border">
    <form class="m-10" action="/create" method="post">
      <input
        class="w-full p-2 rounded-lg bg-zinc-700"
        type="text"
        placeholder="title goes here"
        name="title"
      />
      <textarea
        name="content"
        class="w-full p-2 mt-5 h-20 rounded-lg bg-zinc-700"
        placeholder="write your task here"
      ></textarea>
      <input
        class="mt-2 bg-blue-500 p-2 rounded-md"
        type="submit"
        value="Create Task"
      />
    </form>
    <div class="flex flex-wrap">
      <% if(files.length > 0){ %> <% files.forEach(function(val){ %>
      <div
        class="m-10 bg-zinc-700 rounded-lg pl-2 pr-3 w-72 h-20 flex flex-col justify-between border-2 border-transparent hover:border-zinc-200"
      >
        <p><%= val %></p>
        <div class="flex justify-between">
          <a class="text-blue-500" href="/file/<%= val %>">Read More</a>
          <a class="text-zinc-500" href="/edit/<%= val %>">Edit</a>
        </div>
      </div>
      <% }) %> <% }else{ %>
      <h3 class="text-zinc-200 m-10">No task yet</h3>
      <% } %>
    </div>
  </body>
</html>
